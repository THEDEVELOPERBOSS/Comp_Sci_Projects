# Project Instructions

- This is a dependency-free static website.
- Preserve the dark developer portfolio visual language when editing.
- Use `index.html` for structure, `styles.css` for presentation, and `main.js` for interactions.
- Validate changes by opening `index.html` in a browser.
